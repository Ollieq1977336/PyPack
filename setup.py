from setuptools import setup, find_packages

setup(
    name='PyPack',
    version='0.1',
    packages=find_packages(),
    description='A collection of Tools built for Python',
    author='Ollie Quayle',
    python_requires='>=3.6',  # Specify the minimum Python version required
)
